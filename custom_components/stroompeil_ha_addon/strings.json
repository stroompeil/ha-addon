{
  "config": {
    "step": {
      "user": {
        "title": "Stroompeil HA addon",
        "description": "Connect this Home Assistant to your self-hosted Stroompeil HA server. You get the token by provisioning this host in the Stroompeil HA server dashboard.",
        "data": {
          "server_url": "Stroompeil HA server URL (e.g. wss://server.example.com)",
          "token": "Agent token",
          "host_name": "Display name (optional)",
          "location": "Location (optional)"
        }
      }
    },
    "error": {},
    "abort": {
      "already_configured": "This host token is already configured."
    }
  }
}
